{% extends 'base.html.twig' %}

{% block title %}{{controller_name}}{% endblock %}

{% block body %}
{# <div class="form-panel register">
    <div class="form-header">
    <h1>Créez votre compte </h1>
    </div>
    <div class="form-content">
    <form method="post">
        {% for flashError in app.flashes('verify_email_error') %}
            <div class="alert alert-danger" role="alert">{{ flashError }}</div>
        {% endfor %}
        {{ form_start(registrationForm) }}
                <div class="form-group">
                <label for="email">Nom</label>
            {{ form_widget(registrationForm.name) }}
                </div>
                <div class="form-group">
                <label for="email">Prénom</label>
            {{ form_widget(registrationForm.firstname) }}
                </div>
                <div class="form-group">
                <label for="email">Pseudo</label>
            {{ form_widget(registrationForm.pseudo) }}
                </div>
                <div class="form-group">
                <label for="email">Url de votre image de profil</label>
            {{ form_widget(registrationForm.avatar) }}
                </div>
                <div class="form-group">
                <label for="email">Banniére de profil</label>
            {{ form_widget(registrationForm.banner) }}
                </div>
                <div class="form-group">
                <label for="email">E-mail</label>
            {{ form_widget(registrationForm.email) }}
                </div>
                <div class="form-group">
                <label for="password">Mot de passe</label>
            {{ form_widget(registrationForm.plainPassword) }}
                </div>
                <div class="form-group">
                <label for="cpassword">J'acceptes les conditions d'utilisation</label>
            {{ form_row(registrationForm.agreeTerms) }}
                </div>
                <div class="form-group">
            <button type="submit">Je m'inscris</button>
                </div>
        {{ form_end(registrationForm) }}
    </form>
    </div>
</div> #}
<div class="form">
    <div class="form-panel">
        <div class="form-header">
            <h2>Créez votre compte </h2>
        </div>
        <div class="form-content">
            <form>
                <div class="form-group">
                    <label for="email">Nom</label>
                    <input type="email" id="email" name="email" required="required"/>
                </div>
                <div class="form-group">
                    <label for="email">Prénom</label>
                    <input type="email" id="email" name="email" required="required"/>
                </div>
                <div class="form-group">
                    <label for="email">Pseudo</label>
                    <input type="email" id="email" name="email" required="required"/>
                </div>
                <div class="form-group">
                    <label for="email">Photo de profil</label>
                    <input type="email" id="email" name="email" required="required"/>
                </div>
                <div class="form-group">
                    <label for="email">Banniére de profil</label>
                    <input type="email" id="email" name="email" required="required"/>
                </div>
                <div class="form-group">
                    <label for="email">E-mail</label>
                    <input type="email" id="email" name="email" required="required"/>
                </div>
                <div class="form-group">
                    <label for="password">Mot de passe</label>
                    <input type="password" id="password" name="password" required="required"/>
                </div>
                <div class="form-group">
                    <label for="cpassword">Confirmer le Mot de passe</label>
                    <input type="password" id="cpassword" name="cpassword" required="required"/>
                </div>
                <div class="form-group">
                    <button type="submit">Enregistrer</button>
                </div>
            </form>
        </div>
    </div>
</div>
<div class="form-footer">
    <div></div>
</div>
{% endblock %}
